{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
<nav class="navbar navbar-expand-md navbar-dark sticky-top bg-dark st">
    <div class="container">
        <a class="navbar-brand" href="{{ url('home_page_index') }}">Terminarz <span><i class="far fa-calendar-alt"></i></span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav" aria-controls="nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="nav">
            <ul class="navbar-nav navbar-right">
                <li class="nav-item active">
                    <a class="nav-link" href="{{url('home_page_index')}}">Home <span><i class="fas fa-home"></i></span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('category_index') }}">Kategorie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('event_index') }}">Wydarzenia</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('contact_index') }}">Kontakty</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('tag_index') }}">Tagi</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{url('user_index')}}">Moje Konto</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{url('admin_user')}}">Panel Admina</a>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item ml-5">
                    <a class="nav-link" href="{{url('app_logout')}}">Wyloguj</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% endif %}